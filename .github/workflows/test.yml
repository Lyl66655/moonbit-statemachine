name: test

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  moonbit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup MoonBit
        uses: hustcer/setup-moonbit@v1.23
        with:
          version: 0.10.7+bc794d341

      - name: moon version
        run: moon version --all

      - name: moon check
        run: moon check

      - name: moon build
        run: moon build

      - name: moon fmt --check
        run: moon fmt --check

      - name: moon info
        run: moon info

      - name: moon check --deny-warn
        run: moon check --deny-warn

      - name: moon test --deny-warn
        run: moon test --deny-warn

      - name: moontest
        run: moon test
