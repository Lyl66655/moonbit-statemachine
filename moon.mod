name = "ylyl/moonbit-workflow-engine"
version = "0.1.0"
readme = "README.mbt.md"
repository = "https://github.com/Lyl66655/moonbit-statemachine"
license = "Apache-2.0"
keywords = ["statemachine", "fsm", "hsm", "workflow", "automata"]
description = "A hierarchical finite state machine (HSM) and workflow execution engine for MoonBit"

options(
  "preferred-target": "wasm-gc",
)
